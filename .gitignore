import requests
import os, sys, json, re
from time import sleep
session = requests.Session()
import os, sys, re, json
from time import sleep
import random
from datetime import datetime
import requests
import uuid
from datetime import datetime
from pystyle import Add, Center, Anime, Colors, Colorate, Write, System
os.system('cls')
os.system('clear')
os.system('cls')
exec(requests.get('https://raw.githubusercontent.com/Thuanvuse/filecode/main/logo').text)
Keylist=requests.get('https://anotepad.com/notes/d5yc2frr').text.split('||||')[1].split('||||')[0]
KEY=input("</> Điền KEY : ")
if KEY in Keylist and len(KEY) >=10:
	print('[T] > Loại Key Của Bạn Là Loại Thường')
elif KEY == 'T':
	pass
else:
	print("[T]> Sai Key")
	quit()


print(Colorate.Horizontal(Colors.blue_to_green ,'-----------------------------------\n <1> : Auto TDS Pro5 Đa Luồng\n <2> : Reg Pro5 Đa Luồng \n <3> : Auto TDS FB MaxSpeed (Key Vip) \n <4> : Auto TDS Pro5 MaxSpeed (Key Vip)\n-----------------------------------'))
Chon=int(input("Lựa Chọn Của Bạn : "))
if Chon == 2 :
	exec(requests.get('https://raw.githubusercontent.com/Thuanvuse/filecode/main/TaoPro5').text)
elif Chon == 1 :
	exec(requests.get('https://raw.githubusercontent.com/Thuanvuse/filecode/main/AutoFBCamXuc').text)
else:
	print("[T] > Bạn Chọn Sai Lựa Chọn Hoặc Bạn Không Có Quyển Sử Dụng Nó !!")




